function handleServiceClick(serviceType) {
    if (serviceType === 'general') {
        alert('Redirecting to General Discussion...');
        // Add redirection or modal popup code here
        window.location.href = '#'; // Replace '#' with actual link
    } else if (serviceType === 'help') {
        alert('Redirecting to Help section...');
        // Add redirection or modal popup code here
        window.location.href = '#'; // Replace '#' with actual link
    }
}
